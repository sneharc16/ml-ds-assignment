# Enables deterministic synthetic-data/model bootstrapping in an empty runtime.
# Do not use this switch as a substitute for a persistent production model store.
DRIVEINTENT_BOOTSTRAP_DEMO=0
PORT=8000
OMP_NUM_THREADS=1
OPENBLAS_NUM_THREADS=1
MKL_NUM_THREADS=1
