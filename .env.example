# Optional: override repo root when running from elsewhere (Docker sets this)
DRIVEINTENT_ROOT=/absolute/path/to/driveintent

# Config profile path (defaults to configs/config.yaml)
DRIVEINTENT_CONFIG=configs/config.yaml
